1) შექმენით  Product მასივი 
2) Product მასივში შეიყვანეთ  ობოექტები  (10 ელემენტი)
   
   ობოექტის თვისებებია 
     
     -- დასახელება (string)
     -- ფასი (number)
     -- ფასდაკლება ახალი ფასი (Null/number)
     -- ფოტო გალერეა (array)
     -- ზომები (array) (string) (s,m,l,xl,xxl)
     -- მარაგი (number)
     -- ფასდაკლების პროცენტი (მეთოდი რომელიც დააბრუნებს ფასდაკლების %)

3) გამოიტანეთ  მასივში არსებული ობიექტები html ში შემდეგი პირობებით

   -- გამოიტანეთ მხოლოდ ის პროდუქცია რომელსაც აქვს ფასდაკლება
   -- გამოიტანეთ პროდუქცია ფასი ზრდადობის მიხედვით array.sort()
   -- გამოიტანეთ პროდუქცია რომლის მარაგი არის 0 მეტი

4) პროდუქტის ბლოკებზე გვექნება განსხვავებული html ელემენტები 

   -- თუ პროდუქტის მარაგი არის 5 ზე ნეკლები გამოვიტანთ პროდუქტზე ტექსტს
    (მარაგი იწურება)

   -- თუ პროდუქტის ფასდაკლება მეტია 20% ზე გამოვიტანთ პროდუქტზე ტექსტს
   დღის შეთავაზება

5) პროდუქტის მთავარი ფოტოს არჩევა უნდა იყოს შესაძლებელი ობიექტის 
თვისებებიდან